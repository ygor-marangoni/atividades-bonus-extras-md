# Mais sobre Objetos de Valor (2)

`Temperatura` e um Value Object implementado como `record`. O construtor compacto impede valores abaixo de -273,15 C (zero absoluto) e `toFahrenheit()` aplica a conversao `C * 9 / 5 + 32`.

Execute no diretorio `src`: `javac *.java` e `java Main`.
